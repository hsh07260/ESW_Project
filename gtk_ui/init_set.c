#include <gtk/gtk.h>

gtk_entry_set_text(GTK_ENTRY(weight_entry), NULL);

gtk_entry_set_text(GTK_ENTRY(morning1), NULL);
gtk_entry_set_text(GTK_ENTRY(morning2), NULL);

gtk_entry_set_text(GTK_ENTRY(lunch1), NULL);
gtk_entry_set_text(GTK_ENTRY(lunch2), NULL);

gtk_entry_set_text(GTK_ENTRY(dinner1), NULL);
gtk_entry_set_text(GTK_ENTRY(dinner2), NULL);

gtk_label_set_text(GTK_LABEL(calorie_label), NULL);
gtk_text_view_set_buffer(GTK_TEXT_VIEW(txt_vw), NULL);

